# 票 #47 静态文案清点（全量留痕）

> 基线 `/tmp/flai-os-47` = origin/main @ 39738a8，只读清点（2026-08-05）。
> 标注：【通用】=通用直白；【行话】=工程行话/平台内部概念外漏；【中性】=介于两者之间。
> 本文件是 MAP46-BATCH0-JARGON-AUDIT.md 的证据附件，正文结论以报告为准。

## 1. 登录页与首轮/新用户体验

- 默认路由：登录门过后落 `/` = GuidePage，`meta.title: "对话"`（frontend/src/router/index.js:11）。侧栏主导航仅 `对话`/`今日`（App.vue:254-257）。侧栏品牌行 `二所工程智能体运行底座`（App.vue:19）【行话】。
- WelcomeGate.vue（登录门）：`机器提议，人签发。`（:8）【行话：签发未解释即上首屏】；`二所工程智能体运行底座——任务在这里拆解、执行、留痕。`（:9）【行话】；`欢迎来到 FLAi-OS`（:14）【通用】；`登录后开始工作——签发永远由你亲手完成。`（:15）【行话】；placeholder `用户名`/`密码`、按钮 `登录`【通用】；`没有账户？请联系平台管理员开通（不提供自助注册）。`（:42）【通用】；登录错误直透后端 detail（:69）→ `用户名或密码错误`（backend/app/api/auth.py:63）等【通用】。
- OnboardingCard.vue：组件存在但**全仓零引用**（grep 无 import/使用）——死代码，新用户实际看不到三步引导。其文案备查：`第一次用？3 步上手`（:6）、`跑演示任务`/`演示 →`（:12-13）、`说真实需求`/`输入 ↓`（:17-18）、`人工签发结果`（:22）【行话：签发】、`⌘K 搜任务、会话和 Agent`（:25）。
- EmptyState.vue：props 默认 `暂无数据`（:43）【通用】。调用处传入文案：
  - `没有等你签发的任务`（TodayPage.vue:77）【行话：签发】
  - `当前没有进行中的任务`（TodayPage.vue:107）【通用】
  - `今天还没有交付的任务`（TodayPage.vue:123）【通用】
  - `今天还没有 Agent 动态`（TodayPage.vue:148）【行话：Agent 直出】
  - `本周暂无晋升`（TodayPage.vue:162）【行话：晋升=治理概念】
  - `今天暂无任务`（TodayPage.vue:177）【通用】
  - `本周还没有可统计的团队动态`（TodayPage.vue:201、MePage.vue:85）【通用】
  - `从左栏选择一个任务，或从对话召集一个新任务`（TaskConsole.vue:109）【行话：召集】
  - `还没有可统计的贡献——从对话发起第一个任务吧`（MePage.vue:28）【通用】
  - `还没有任务——先在主对话里说明目标，系统会自动编排。`（TaskConsole.vue:88）【通用偏中性】
  - `暂无可用 Agent`（AgentPortal.vue:46）【行话】
  - `暂无反馈`（TaskDetail.vue:290、FeedbackPage.vue:78）【通用】
  - `暂无事件`（WorkLog.vue:75）【通用】

## 2. 会话面（GuidePage.vue 及子组件）

**起手 hero（默认可见，零消息态）**：
- 时段问候 `早。/午安。/下午好。/晚上好。/夜航中？/夜深了，辛苦。`（:900-904）【通用】
- `说说你要做的工程活儿`（:11）【中性：口语化但锚定工程】
- `输入文字或上传附件，系统会在后台自动编排所需能力。`（:13）【中性：编排】
- placeholder `描述工程需求…`/`继续说下去…`（:886-887）；对账锁态变体 `正在核对失败任务…`/`创建状态待核——请先核对本次开工`/`保存状态待核——请先刷新会话核对`（:880-884）【行话：待核/开工】
- composer 常驻政策句 `系统会在后台准备方案 · 开工与签发仍由你确认`（:586）【行话：方案/开工/签发三词同屏】；变体含 `审计血缘会自动保留`（:586）【行话：血缘】
- 快捷键行 `Enter 发送·⇧ Enter 换行·可带附件`（:587）【通用】

**工作段分隔（默认可见）**：`▸ N 轮往来 · 工作段 X（时刻）`（:39）、`工作段 X · 时刻`（:40）【行话：ADR-0033 内部概念直出】。

**方案卡（默认可见面）**：
- kicker `协作方案`（:143）；`已自动编排 · N 个执行单元`（:148）【行话：执行单元】
- 状态行 `复用证据无法核验，本次禁止开工；请继续对话让系统重新编排`（:153）、`方案有执行单元未能纳入…`（:158）、`创建状态待核，禁止重复开工`（:159）、`信息已齐，等待你确认开工`（:160）、`执行已接入`（:161）、`还需通过对话补充执行信息`（:162）
- 编队总览行（可见面，squad.js）：`协作已收束 · 用时 X`（:59）、`N 位专家协作`（:63）、`N 运行中`（:64）、`N 已入队`（:65）、`N 等待接力`（:66）【行话：接力】、`N 待你签发`（:67）、`N 已完成/N 失败/N 已取消`（:68-70）；`审阅签发 →`（GuidePage.vue:189）
- 成员行 `输入已自动整理 · 待开工`（:272）、`等待对话补充`（:273）、`已召集 · 接入中…`（:275）【召集】；旁白 `等待〈上游成员〉的产物 · 就绪后自动接力`（:2675）、`前序失败，接力已暂停 · 详情→`（:2672）
- stageline `排队中——等待执行器领取`（:1799）、`产物已就绪——等待你审阅放行`（:1801）、`任务完成 · 用时 X`（:1804）、`运行失败——点右侧速览看详情（如实透出，不静默）`（:1806）【中性偏通用】
- 事件旁白 `校验输入契约…`（:1646）【行话：契约】、`前序产物已就绪，接力开始`（:1655）、`· 残差 X`/`· 累计 N 次迭代`（:1664-1665）【行话：残差/迭代=航空仿真语】
- 依据 chip `依据 N 条（X 已核验 · Y 未核）`/`置信度 X（模型自评）`（:309）【中性】、`依据清单〔按密级隐藏〕`（:315）【行话：密级】
- 拒答行 `已如实说明：N 项超出能力范围 →`（:330）【通用】
- 底部政策句 `系统会在后台自动编排所需能力；开工由你确认，产物放行由你批准——签发权始终在你。`（:430）【行话：开工/签发权】
- 主按钮 `按方案开工`/`正在开工…`（:413）【行话】、`进入协作工作台 →`（:418）

**route-disclosure 折叠披露区（默认收起）**：
- summary `查看路由依据与边界`（:194）【行话：路由，全站唯一默认可见入口（折叠 summary 常显）】
- `复用方法来源`（:198）、`隔离包 X · 已通过包级人工复核…`（:201-202）【行话：隔离包】
- `分工如何衔接`（:206）、`执行单元 · N`（:209）、`使用材料`（:282）
- `已剔除不合法字段：…（未匹配该执行单元的输入契约）`（:349）【行话】
- `系统还缺少执行所需信息——继续说明目标、材料或约束即可，不需要填写字段。`（:352）【通用】
- 分类学悬浮披露（agent-name 的 title/aria-label，非默认视觉）：`领域 X`+`密级 Y`+`成熟度 Z`（:2729-2732；词表 `制度/标准/故障史/系统计算/CFD 仿真/试验数据/设计优化/通用` :2694-2701，`公开/内部/敏感` :2703）【已收 title】

**refuse 拒答卡（默认可见）**：
- kicker `显式拒绝`（:93）【行话：decision 枚举直出】；`这个需求，平台暂时接不住`（:94）【通用】；`你手上仍未解决的问题`（:100）【通用】；`可以试试这样重述 / 拆解`（:106）、`采纳 →`（:120）；`或者直接在下方输入框，告诉导引你想怎么调整。`（:123）【导引自称】
- `复制需求摘要`（:129）；`接不住 ≠ 不重要——复制摘要发给平台负责人登记，平台会按家底口径统一评估排期。`（:130）【通用；家底口径内部词】
- 复制产物 `【需求登记草稿】…（来自 FLAi-OS 导引对话——请平台负责人按家底口径评估排期）`（:1044-1056）；toast `需求摘要已复制——发给平台负责人登记，别让它溜走`（:1058）【通用】

**错误/异常文案**：
- 流式中断卡 `流式中断 · 保存状态待核`（:77）、`刷新会话核对`/`核对中…`（:88）、`已停止 · 本轮未保存`（:1164）【行话：待核/核对】
- 报错 toast（实测 [role=alert]）：`流式中断 · 本轮未保存 — 导引本轮对话失败（可重试）：{上游原始错误文本}`（conversations.py:204 直通 `exc`）【前缀人话，后缀直通上游】
- 慢等待真话 `内网大模型推理较慢——复杂需求可能要一两分钟。请留在本页稍候：本轮若失败，你的原话会退回输入框，不会丢。`（:492，触发条件 thinkingSeconds>=30）【通用，正面范本】
- 恢复入口 `恢复入口无效——已按普通对话打开，不会写入重跑血缘`（:744）、`该任务不是失败态——已按普通对话打开，不会伪造重跑血缘`（:759）【行话：血缘】
- `已停止生成——本轮未保存，原话已退回输入框`（:1432）、`服务端确认该轮未保存——原话已退回输入框`（:3031）【通用】
- 开工门槛 toast：`Skill 复用证据无法核验，本次未创建任务…`（:1949）、`方案信息尚未全部就绪…`（:1959）、`执行单元输入契约已更新或暂时无法核对——本次未创建任务…`（:1987）【行话】、`附件归属尚不能唯一确定…`（:1999）、`无法安全保存本次开工标识…`（:2034）
- 开工结果：`已按方案召集 N 名成员——进度与签发都会来这里找你`（:2080）、`创建状态待核——无法确认任务是否已写入；本方案已锁定，请用同一操作标识核对，禁止重复开工。`（:2096）【行话密集】、`召集未执行（全有全无，未创建任何任务）`（:2142）【行话：事务术语直出】、`已核对：原开工请求已创建 N 个任务，没有重复创建`（:2196）、`创建状态仍待核——原操作标识已保留，禁止换 key 重复开工。`（:2217）【行话：换 key】
- 资产批八：`已接受为资产候选；尚未登记或发布`（:2522）、`必须先核验并审阅当前隔离包的真实字节，才能批准复用。`（:2561）【行话：隔离包】
- 页级兜底 `会话加载失败`（:2988）、`无法核对：当前会话标识缺失，请从左侧历史重新打开会话`（:3003）

**FeatureAssetMapDisclosure（默认折叠）**：`FLAi-OS 功能与资产地图`（:7）、`按需查看平台能力与你的受治理资产`（:8）、`N 项能力 · N 份资产`（:11）、`只读披露`（:13）、`正在冷读并核验功能与资产来源…`（:18）【冷读】、`不执行 · 不注册 · 不晋级`（:33-35）【晋级】、`Registry 能力`（:45）【英文内部词】、`我的 Candidate`（:49）【英文】、`隔离 Skill 包`（:53）、`从 Agent Registry 冷读，不推断可用性`（:65）、`类型未声明/领域未声明/状态未知 · 成熟度未知`（:79-84）、`需人工复核`（:85）、`N 个 mock 工具引用`（:91）、`Workflow 未形成/Agent 未形成`（:124-125）【英文直出】、`可进入任务级匹配；仍需开工与签发`（:130）、`当前账号还没有可冷读核验的资产 Candidate。完成任务不会自动等于资产；只有证据闭合后才会在这里出现。`（:135）

**AssetCandidateCallout（任务完成后长出，默认可见）**：`可复用资产`（:10）、`任务完成后自动形成`（:50）、`你只需判断这套方法值不值得留下；系统内部如何路由，不需要你填写或选择。`（:52）【通用；路由语境自然】、`系统归纳的方法`（:71）、`人必须保留的判断`（:78）、`已从接受的候选确定性生成隔离包待复核…`（:116）【隔离包】、`达到数量门也不会自动形成 Workflow 或 Agent Candidate。`（:127）【英文概念】、`签发证据`（:172-179）等

**GuidePage 内术语频次（全文件含注释/非注释近似）**：Agent 38/25、开工 45/28、方案 54/18、召集 29/9、编排 12/10、签发 10/6、工作段 8/2、契约 13/5、密级 8/4、路由 9/2、执行单元 8/6、本体 1/0。

## 3. 任务台/今日页

- TodayPage.vue：页题 `今日`（:8）；行动召唤 `N 项待你签发`（:34）【签发】；版块 `待你签发 · N`（:45）、`进行中 · N`（:84）、`今日交付 · N（显示最近 12 条）`（:118）、`Agent 动态`（:133）、`今日最活跃 Agent`（:170）【Agent 直出】、`团队总量`（:188）；行 meta `{agent_id} · 需要你签发 · HH:MM`（:103）【裸 agent_id】；晋升行 `{agent_id} 晋升 L0 · 原型 → L1 · 试用`+`签发人 X`（:156-158）【晋升/签发人】；统计格 `本周完成`/`本周人签放行`/`累计固化 case`（tip `按仓内固化文件计`）/`本周晋升`（:323-327）【固化 case】；页脚 `基于最近 100 条任务窗口`（:206）【偏技术】。
- TaskConsole.vue：栏题 `任务台`（:8）；`发起新对话`（:9）；组头 `✍ 待你签发 · N`（:23）、`进行中 · N`（:47）、`已落定 · N`（:69）【通用】；行 meta `{agent_id} · {状态} · HH:MM`（:40/62/84）【裸 id】；未读点 title `完成后你还没看过`（:64/86）【通用】；空态 `还没有任务——先在主对话里说明目标，系统会自动编排。`（:88）；页脚 `清单来自最近任务窗口（100 条）真实轮询——窗口外不虚报。`（:91）【偏技术】。
- TaskDetail.vue：`← 返回协作会话`（:19）；徽章 `待你签发`（:43）；敏感声明 `敏感数据任务——执行期接触了受限文件、语料或工具产出，产物及其内容请按部门数据口径流转，不外发。`（:32）【密级/数据口径，安全要求】；meta `重跑自 {id}`（:84）、`接力自：{上游名}`（:91）【接力】；失败行 `回到对话说明问题`（:120）+`直接补充文字或附件，系统结合原会话重新编排——平台不会自动重跑。`（:121）【通用】；`产物 · 放行前请先审阅`（:129）【放行】；`数据与日志类产物已默认收纳 N 件——先读报告，展开即可逐项核对原始数据。`（:184）【通用】；`本次输出未提供依据，请谨慎签发`（:200）；`依据清单〔按密级隐藏〕——原文未拉取，签发前请经授权渠道核对`（:205）【密级】；动作段 `审核人 {signer}（登录身份，签发记名不可代填）`（:230-231）、`批准即代表你作为工程师背书该产物——签发权在你，平台不代签。`（:237）、`授权链：X 于 T 创建本任务；除你此刻的批准外，平台没有任何自动放行路径。`（:243）【授权链】、`批准放行`/`驳回`（:248-250）【放行】；rail `模型调用`/`tokens 合计 X`/`部分调用上游未回报 token，合计为下界`（:358-378）【token 行话但工程产品常见】；`知识引用——执行期知识检索命中的语料块——签发前可回源核对原文。`（:387-393）【语料/回源】；`指纹 X`（:414）【指纹】；`语料在检索后已变动：检索时指纹 X，与当前不一致——请以当前原文为准重新核对。`（:419）。
- TaskJourney/taskJourney.js：`执行链`（:11）、`从输入到交付的真实状态一眼可见；普通完成不等于已验证，只有具名人工批准才显示签发标记。`（:12）；节点 `输入/Agent 执行/工具 · 模型/产物/人工签发/交付`（:179-188）；`等待人工签发`（:136）、`X 已批准放行`（:133）、`待人工放行`（:150）。
- format.js（SSOT）：十态 `已创建/排队中/校验中/运行中/等待人工审核/解析中/分析中/已完成/失败/已取消`（:4-18）【通用】；事件译文 `Agent 运行日志`/`模型调用`/`知识检索`/`请求人工审核`/`人工已批准`（:45-64）；Agent 分类 `工具自动化型/知识问答型/结构化生成型/推理辅助型`+tip `编排工具批量作业，如性能盘计算`/`基于受控知识范围回答工程问题`/`按规则生成结构化产物，如控制逻辑`/`LLM 辅助推理出草案，结论需人工确认`（:73-78）【性能盘/控制逻辑/LLM】；成熟度 `L0 · 原型`（tip `L0 原型：能力验证阶段，勿依赖其结论。`）…`L3 · 成熟`（:95-100）【L0-L3】；口播 `✓ 由 X 批准放行`/`✕ 由 X 驳回`（:238-239）。
- VerificationCard.vue：`核验三面`（:12）、`N 次工具调用 · 含 N 次 mock`+`未经真实核验`（:25-26）【mock 直出=宪法诚实标注】、**地板句现状** `通识解释仅供参考；工程结论以确定性工具与人签为准`（:71，#36 落点，注释 :67-68 逐字说明松弛的是频率不是诚实纪律）。
- CompletionSeal/DeliveryCard：`已完成/执行失败/已取消`、`工作 X 分 X 秒`/`进行了 X`；`模型调用：未知`/`N 次调用 · token 用量：未知`。

## 4. StatusDock / StatusCenter / QuickSwitcher / MePage / AgentPortal

- StatusDock.vue：pill `✍ 待你签发 N`（:6）、`运行中 N`（:10）；签发提醒 toast `「{任务名}」待你签发`（:104）。
- StatusCenter.vue：`状态中心`（:23）、`状态来找你——这里汇总要你处理与正在发生的`（:24）【通用】；组 `✍ 待你签发 · N`（:36）、行 `审阅 →`（:47）、空态 `没有等你签发的任务`（:52）、`运行中 · N`（:58）、`(等待接力)`（:72）、`最近落定`（:81）、`查看全部任务 →`（:93）；页脚 `口径：计数与清单均来自最近 100 条任务窗口，窗口外不虚报。`（:97）；速览固化 `N 条样本已认可，可固化为评测用例`+按钮 `固化`（:136-142）【固化/评测用例】；签发卡 `签发`（:201）、`批准即代表你作为工程师背书该产物——签发权在你，平台不代签。`（:202）、`授权链：…`（:205）；确认框 `确认批准放行该任务？`+`再看看`（:611-616）。
- QuickSwitcher.vue：`搜索会话、任务…`（:20）【通用】、组名 `会话`/`任务`（:180-181）、降级条 `后端搜索请求全部失败——以下显示可能不完整`（:43）、空态 `没有匹配结果`（:104）、footer `↑↓ 选择 · ↵ 打开 · esc 关闭`（:109）。
- MePage.vue：`我的贡献`（:4）、`仅你本人可见 · 只统计可精确归因的贡献`（:5）【通用】；格 `本周发起/本周完成/待我跟进/累计发起`（:125-128）【通用】、`本周签发/累计固化/本周晋升`（:138-141）【签发/固化/晋升】；`我的反馈——按显示名近似统计，可能与同名者混计`（:68-69）【通用诚实】；`团队总量仅作氛围对照，不含任何人际排名`（:86）【通用】；诚实缺口条 `此页只统计可精确归因的发起贡献；签发/样本认可的归因待后续（人是唯一签发者，签发身份现仅留审计轨）。`（:92）【签发/审计轨】。
- AgentPortal.vue：`Agent 能力与治理`（:7）【行话】、`只读了解能力、边界与成熟度；实际任务由主对话自动路由`（:8）；降级条 `Agent 目录仍可浏览，但能力关系与引用状态不作展示。`（:23）；`Agent 本体投影契约不兼容`（:500）/`Agent 本体投影加载失败`（:504）【本体/契约，默认可见错误 alert】；卡片 `密级上限 · 敏感`（:80）、`成熟度 · L0`（:105）、`{agent_id} · v{version}`（:107）、`不适用范围 · N`（:115）、`由系统按任务语义自动编排`（:126）；专家团队 `只读蓝本——实际组合由主对话按目标自动编排`（:138）；治理弹窗 `成熟度`（:175）、`L2/L3 范围外：当前仅 L0→L1 由机器把关晋升`（:189）、`评测通过率（近 N 次）`（:207）、`待策展（不计入评测）`（:265）【策展】、`已固化 N 个 eval case（按仓内固化文件计）`（:283）【eval case】、`申请晋升 L1`（:308）、`准入判定快照`（:335）；usefulness 人话轴 `L1 帮我省事`/`L2 比我聪明`/`L3 带我做`（:416）+tip `L1 帮我省事：机械劳动代劳，人全程可核对`等（:420-422）【人话化范本】。

## 5. guide_agent 提示词与姿态体系（agents/guide_agent/prompt.md，131 行）

- 身份调性（:1-4）：`你是 FLAi-OS 平台的「导引 Agent」，二所工程智能体运行底座的**统一入口与编排官**。面对你的往往是不关心「什么是 Agent」的领导或同事——他们只想把一件繁琐的工程活儿交出来…`
- 第四姿态（#33 通识通道）逐字（:15-32）：`默认接——能 delegate/orchestrate/通识直答就接；refuse 仅在平台能力边界或工程判断纪律确实接不住时；「不熟/模糊」不构成门槛…`（:16-17）；`**通识/概念解释** → 叙事通道直答（**不输出计划块**）：术语、方法论、原理、流程概念等非工程判断问题，直接给出简明解释；地板句不再逐答复述，改由批准/授权面统一展示（#36）。直答不得含具体工程数值结论、合格性判定或签发口径；若问题实质要工程判断，照 orchestrate/refuse 处理。如果你发现自己在心里把一个实质要工程判断的问题**重新解释**成通识问题来直答，这个重新解释本身就是信号：照 orchestrate/refuse 处理，而不是放行。按整段对话的**累积输出**判定，不逐轮孤立…`（:22-30）；`把通识问题扫进拒绝是过度拒绝，同样失职——通识解释属宪法允许的叙事/解释通道。`（:31-32）
- #36 地板句松弛现状：prompt 仅存 `地板句不再逐答复述，改由批准/授权面统一展示（#36）`（:23-24）；落点=VerificationCard 常驻行（上引）。
- #42 Opus 5 条款：出处=backend/tests/test_guide_auto_routing_contract.py:1257-1265 `test_opus5_clause_anchors_in_prompt`——锚 `默认接`/`重新解释本身就是信号`/`累积输出` 三串逐字入 prompt.md（:16-17/:26-30），对应 #41（Opus 评审）裁决的三条可搬条款，#42 施工落 prompt 并以测试钉死字面。配套探针 eval case_004（累积判定）、case_005（refuse 语气施压不退让，对应 prompt :126-127 `认账并修正真正错的部分，但不因施压退让签发口径——重新裁决只依据新事实，不依据情绪`）。
- 全仓 grep -ri "opus"：除上述测试锚外，命中均为提交署名/评审留痕（Claude Opus 4.8）；contracts/agents 下零命中——「Opus 5 条款」=评审者给出的条款，非模型配置条款。

## 6. 后端浮到 UI 的用户可见错误/拒答字符串（节选）

- 会话路由：`agent {agent_id} 非 interactive 型，不能发起会话（请走 /api/tasks 一次性任务）`（conversation.py:292-294）【interactive】；`会话所属 agent Registry 不支持不可变执行视图`（:460-462）【Registry/不可变执行视图】；`会话历史已达到安全上限，本轮未执行且未落库`（:116-118）【落库】；`会话在本轮生成期间工程工作段边界已变化，本轮不落库`（:832-834）【工作段】；`流式客户端已断开，本轮不落库`（:238 等）【流式/落库】
- 密级门：`该专家的密级准入上限为「X」，无法处理「Y」级附件——请改派密级上限足够的 Agent 或移除受控附件（ADR-0030）`（conversation.py:581-584）【密级+ADR 编号直出】
- 模型网关：`模型网关未配置，导引不可用：{exc}。此为部署配置问题（非临时故障），请联系管理员设置 FLAI_LLM_* 环境变量后再试。`（conversations.py:198-199）【环境变量名直出】；`导引本轮对话失败（可重试）：{exc}`（:204）【上游错误直通】
- 开工门槛：`Agent 不可变包快照不可用，任务未创建`（tasks.py:752）【不可变包快照】；`整批未创建（全有全无）——逐项修正后重试`（:1231）；`agent 版本在对账后发生变化：X v1 → v2——请重新核对后开工`（:1407-1409）【对账】；`创建操作 {operation_id} 已被不同载荷使用或记录不一致——禁止覆盖；请先核对原任务`（:730-733）【operation_id】
- 取消/签发：`任务处于 {status}，不在 waiting_review，无法人工放行/拒绝`（:1785）【英文枚举直出】
- 权限 404 塌缩：`资源不存在或不可访问`（object_authorization.py:22，防存在性 oracle，统一塌缩）【通用】
- 文件：`文件分级 X 未开放下载：角色授权体系未建立前非 internal 数据一律 fail-closed 拒绝（ADR-0021）`（files.py:326-329）【fail-closed 英文+ADR 编号】；`文件超出上传限额（FLAI_MAX_UPLOAD_MB，当前=N 字节）`（:232）
- 知识回源：`该知识范围为受限密级，原文回源在角色轴落地前一律不放行`（knowledge.py:63）【角色轴】；`scope X 当前语料中不存在 chunk Y…`（:91-93）【scope/chunk】
- worker 上浮：`worker_interrupted：上次 worker 进程中断，任务未完成即失败；真实工具可能已产生外部副作用，请人工核查后重建任务`（runner.py:52-53）；级联取消 `依赖的上游 completed 但产物未过人工签发闸（K1 签发维 provenance），级联取消（fail-closed 未签判决绝不越界）`（:348）【provenance/fail-closed】
- 任务失败面（实测 c3）：`ModelConfigError: 缺少环境变量 FLAI_LLM_BASE_URL（profile=reasoning 的 base_url，见 docs/04）`——异常类名+环境变量名+内部文档指针直出默认面（事件时间轴同文案复读）

## 7. 工程术语频次（frontend/src，*.vue+*.js；全部命中/非注释近似）

| 词 | 全部 | 非注释 | 代表性用户可见 file:line |
|---|---|---|---|
| Agent | 384 | 309 | `暂无可用 Agent` AgentPortal.vue:46；`Agent 动态` TodayPage.vue:133 |
| 签发 | 157 | 96 | `待你签发` TodayPage.vue:45/TaskConsole.vue:23/StatusCenter.vue:36；`签发权在你，平台不代签` TaskDetail.vue:237 |
| 方案 | 83 | 28 | `协作方案` GuidePage.vue:143；`按方案开工` :413 |
| 开工 | 69 | 41 | `按方案开工` GuidePage.vue:413；`信息已齐，等待你确认开工` :160 |
| 路由 | 38 | 10 | `查看路由依据与边界` GuidePage.vue:194；`实际任务由主对话自动路由` AgentPortal.vue:8 |
| 密级 | 37 | 23 | `依据清单〔按密级隐藏〕` GuidePage.vue:315/TaskDetail.vue:205 |
| 契约 | 36 | 16 | `未匹配该执行单元的输入契约` GuidePage.vue:349；`校验输入契约…` :1646 |
| 导引 | 36 | 8 | `告诉导引你想怎么调整` GuidePage.vue:123 |
| 任务台 | 34 | 11 | `任务台` TaskConsole.vue:8 |
| 会话 | 261 | 84 | `会话` QuickSwitcher.vue:180；`与 X 的对话` conversationTitles.js:54 |
| 工作段 | 14 | 2 | `工作段 N · HH:MM` GuidePage.vue:39-40 |
| 本体 | 11 | 8 | `Agent 本体投影契约不兼容` AgentPortal.vue:500 |
| workflow/Workflow | 55 | 45 | 可见面仅 `Workflow 未形成` FeatureAssetMapDisclosure.vue:124 等披露区 |
| 白名单 | 4 | 0 | 无可见文案（全在注释） |
| 已审定 | 1 | 1 | ui-lab fixture 文案，非生产面 |

## 8. agents/ 16 包用户可见称呼（agent.yaml name；无 display_name 字段）

| id | name（用户可见） | 领域 |
|---|---|---|
| cfd_evaluate_agent | CFD 结果评估 Agent（Strouhal/Cd 确定性判据 + 叙事草案） | 航空 CFD |
| cfd_solve_agent | CFD 求解发起 Agent（真实 OpenFOAM，fire-and-register） | 航空 CFD |
| control_logic_agent | 控制逻辑结构生成 Agent | 控制工程 |
| fault_history_agent | 历史故障检索 Agent | 航空维修/故障史 |
| fea_evaluate_agent | FEA 梁频率评估 Agent（闭式解析解确定性判据） | 结构有限元 |
| fea_solve_agent | FEA 梁固有频率求解 Agent（纯 Python 有限元） | 结构有限元 |
| fta_agent | 故障树分析辅助 Agent（FTA 草案生成） | 可靠性工程 |
| guide_agent | 导引 Agent（需求接待与协作编排） | 平台入口 |
| hello_agent | Hello Agent（平台闭环验证示例） | 平台自验 |
| knowledge_qa_agent | 知识问答 Agent（语料检索归纳草案生成） | 通用知识问答 |
| monitor_adapter_gen_agent | 监控接入生成器 Agent（adapter 草案） | 仿真监控工具链 |
| performance_disk_agent | 性能盘批量计算 Agent（模拟阶段） | 航空性能盘 |
| policy_qa_agent | 制度政策问答 Agent | 所内制度 |
| standards_qa_agent | 专业标准问答 Agent | 专业标准 |
| step_response_evaluate_agent | 阶跃响应超调评估 Agent（闭式解析解确定性判据） | 控制工程 |
| step_response_solve_agent | 二阶系统阶跃响应求解 Agent（纯 Python 梯形积分） | 控制工程 |

具体程度：16 称呼全部以「Agent」结尾；15/16 带工程/航空限定词；通用向仅 knowledge_qa/policy_qa/standards_qa/hello/guide 五个，且 standards_qa 能力描述仍锚「合成机型实例」。多数称呼直接带方法学缩写（Strouhal/Cd、OpenFOAM、Euler-Bernoulli、FTA、adapter、fire-and-register）——非航空工程师不可读。**无任何通用执行型 Agent（脚本/表格/翻译/文档）。**

## 行话密度观察（结论节录）

- 默认可见面仍带平台内部概念：**签发**（157 处，全站最高频内部词，登录门/组头/政策句/签发卡）、**开工**（69 处，主 CTA 与对账文案）、**工作段**（会话轴段头）、**召集/接力/编排/执行单元**（方案卡可见面）、**路由**（折叠 summary 与门户副题）、**密级**（安全要求使然）。
- 后端直出技术词到 toast/alert：waiting_review、decision=orchestrate、fail-closed、provenance、ADR 编号、FLAI_LLM_*/FLAI_MAX_UPLOAD_MB、chunk_id/scope、operation_id/换 key、不可变包快照、resolver 管道。
- 已人话化的面：主叙事动作口语（`说说你要做的工程活儿`/`这个需求，平台暂时接不住`）；usefulness 轴 L1-L3 人话；事件类型全翻译，snake_case 不裸显；拒答措辞去技术化；术语进披露纪律（图例句撤下、释义挂 title、成员分类学收 title）。
- 「Agent」一词全量直出（384 处）未翻译，是当前最重的平台原生词；任务行主名已用 taskDisplayName 人话遮盖，副行仍裸 id。
- OnboardingCard.vue 零引用=死代码，新用户实际没有 onboarding 面。
