# performance_disk_agent · Prompt

**V0.1 本 Agent 不调用任何模型**（`agent.yaml` `model.profile: none`）：
workflow 为确定性工具编排，报告由纯 Python 模板生成。本文件是 docs/02
标准包形态的强制件，当前作为占位与 V0.2 规划记录存在——不含任何生效指令。

## V0.2 预留（LLM 摘要，届时启用本文件并升版本）

计划用途（全部限「叙事/建议/解释」通道，宪法第四条边界）：

- 对 `task_report.md` 的失败 case 清单做自然语言归纳（如"高海拔 case 集中失败"）；
- 对解析行级错误给出修表建议。

红线（届时写入正式 prompt）：

- 禁止改写/推断任何数值结果——数值只来自工具输出的忠实转写；
- 禁止对结果做工程结论判定（判定权在确定性代码 + 人）；
- mock 阶段的一切摘要必须显式携带"结果无工程意义"声明。
