# cfd_solve_agent · Prompt

**本 Agent 不调用任何模型**（`agent.yaml` `model.profile: none`）：workflow 为
确定性工具编排（cfd_solve_launch 发起真求解，fire-and-register），无 LLM 参与。
本文件是 docs/02 标准包形态的强制件，作为占位存在——不含任何生效指令。

若未来需要 LLM（如发起前的算例参数叙事确认），启用本文件并升版本、记 changelog。
