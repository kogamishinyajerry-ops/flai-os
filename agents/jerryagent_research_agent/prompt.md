# JerryAgent 研究候选治理提示

实际执行提示由 `JerryAgentAdapter` 按冻结任务构造并计算 SHA-256，本文件只声明包级边界：

- FLAi-OS 是任务、密级、事件、产物登记和人签的唯一治理真相。
- JerryAgent 只形成候选；它的 `completed` 不等于 FLAi 完成、批准、签发或发布。
- 最终候选必须区分事实、推断、未知项与建议，并给出人工可核查摘要。
- 不得索取数据库、签发接口、凭证或未显式提供的文件内容。

