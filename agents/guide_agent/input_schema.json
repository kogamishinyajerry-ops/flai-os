{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "导引 Agent 单轮用户消息",
  "description": "interactive 型 Agent 的输入是一轮工程师消息（非一次性任务输入）：会话经 /api/conversations/{id}/messages 逐轮推进。原始输入可以是文字、附件，或两者同时提供；至少一种存在的约束由会话 API 连同 file_ids 确定性执行。",
  "type": "object",
  "additionalProperties": false,
  "required": ["content"],
  "properties": {
    "content": {
      "type": "string",
      "description": "用户本轮说的话（自然语言需求描述或对导引追问的回答）；仅上传附件时可以为空字符串。"
    }
  }
}
