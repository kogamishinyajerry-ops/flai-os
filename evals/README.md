# evals/ — 跨 Agent 评测骨架（任务书 §5 长期资产槽位）

V0.1 各 Agent 的评测用例**单一事实源在包内** `agents/<agent_id>/eval_cases/`
（随包版本化，注册期强制存在）。本目录为任务书 §5 预留的三业务子目录骨架，
用于后续**跨 Agent 组合评测 / 回归基线快照**（docs/07 Eval 标准的 V0.2 落点），
当前刻意为空——不放副本（复制必腐烂）。
